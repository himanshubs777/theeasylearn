# write a programe to calculate the area of triangle
# formula = 1/2 * base * height

base = 0
height = 0

base = float(input("Enter the base:"))
height = float(input("Enter the height:"))

area = 0.5 * base * height

print("Area of triangle is :", area)